// import { doc } from 'firebase/firestore';

const planType = {
    text: String,
    type: String,
    value: Number,
    // use referencia do id da coleção, no caso:
    // const planId = doc(db, 'plans_refference', 'planIdRef'); 
    // plan: planId
}